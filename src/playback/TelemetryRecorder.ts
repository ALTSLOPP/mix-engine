/**
 * DEPRECATED — moved to `src/sensorium/TelemetryRecorder.ts`. Shim kept for
 * backward compatibility. Import from `../sensorium` in new code.
 */
export { TelemetryRecorder } from '../sensorium/TelemetryRecorder';
export type { TelemetrySnapshot, TelemetryEvent } from '../sensorium/TelemetryRecorder';
