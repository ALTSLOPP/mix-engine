/**
 * DEPRECATED — PLAYBACK has been superseded by SENSORIUM.
 *
 * This file is a thin compatibility shim. All types now live in
 * `src/sensorium/types.ts`. Import from `../sensorium` in new code.
 */
export * from '../sensorium/types';
